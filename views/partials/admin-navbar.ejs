<%
  const current = typeof activePage !== 'undefined' ? activePage : '';
  const signedInUser = typeof adminUser !== 'undefined' && adminUser ? adminUser : null;
  const roleLabels = {
    superadmin: 'แอดมินใหญ่',
    admin: 'Admin',
    team_leader: 'Team Leader',
    agent: 'Agent',
  };
  const roleBadgeLabel = signedInUser ? (roleLabels[signedInUser.role] || 'ผู้ดูแลระบบ') : '';
  const userPermissions = Array.isArray(signedInUser?.permissions) ? signedInUser.permissions : [];
  const canAccess = (permission) => {
    if (!permission) return true;
    if (!signedInUser) return true;
    if (signedInUser.role === 'superadmin') return true;
    return userPermissions.includes(permission);
  };

  const pageTitles = {
    dashboard: 'แดชบอร์ด',
    categories: 'จัดการข้อมูล',
    settings: 'ตั้งค่า',
    settings2: 'ตั้งค่า',
    'instruction-ai': 'InstructionAI2',
    'instruction-ai2': 'InstructionAI2',
    'instruction-chat': 'Instruction Chat',
    'api-usage': 'สถิติ API',
    chat: 'แชท',
    orders: 'ออเดอร์',
    followup: 'ติดตามลูกค้า',
    broadcast: 'บรอดแคสต์',
    'facebook-posts': 'โพสต์ FB / คอมเมนต์',
    'facebook-comment': 'คอมเมนต์ Facebook',
    'customer-stats': 'สถิติลูกค้า',
  };
  const pageTitle = pageTitles[current] || 'จัดการ AI';

  const navGroups = [
    {
      title: 'AI & ระบบ',
      items: [
        { page: 'dashboard', label: 'แดชบอร์ด', icon: 'fa-tachometer-alt', href: '/admin/dashboard', permission: 'menu:dashboard' },
        { page: 'settings2', label: 'ตั้งค่า', icon: 'fa-cogs', href: '/admin/settings2', permission: 'menu:settings' },
        { page: 'instruction-ai', label: 'InstructionAI2', icon: 'fa-wand-magic-sparkles', href: '/admin/instruction-ai', permission: 'menu:instruction-ai' },
        { page: 'api-usage', label: 'สถิติ API', icon: 'fa-chart-line', href: '/admin/api-usage', permission: 'menu:api-usage' },
      ],
    },
    {
      title: 'ลูกค้า',
      items: [
        { page: 'chat', label: 'แชท', icon: 'fa-comments', href: '/admin/chat', badgeId: 'chatNotificationBadge', permission: 'menu:chat' },
        { page: 'orders', label: 'ออเดอร์', icon: 'fa-shopping-basket', href: '/admin/orders', permission: 'menu:orders' },
        { page: 'followup', label: 'ติดตามลูกค้า', icon: 'fa-user-clock', href: '/admin/followup', permission: 'menu:followup' },
        { page: 'broadcast', label: 'บรอดแคสต์', icon: 'fa-bullhorn', href: '/admin/broadcast', permission: 'menu:broadcast' },
        { page: 'facebook-posts', label: 'FB โพสต์/คอมเมนต์', icon: 'fa-comment-dots', href: '/admin/facebook-posts', permission: 'menu:facebook-posts' },
        { page: 'customer-stats', label: 'สถิติลูกค้า', icon: 'fa-chart-bar', href: '/admin/customer-stats', permission: 'menu:customer-stats' },
      ],
    },
    {
      title: 'อื่น ๆ',
      items: [
        { page: 'categories', label: 'จัดการข้อมูล', icon: 'fa-database', href: '/admin/categories', permission: 'menu:categories' },
      ],
    },
  ];

  const isActive = (page) => {
    if (!current || !page) return false;
    if (page === 'settings2') return current === 'settings' || current === 'settings2';
    if (page === 'instruction-ai') return current === 'instruction-ai' || current === 'instruction-ai2' || current === 'instruction-chat';
    return current === page;
  };

  // Mobile bottom nav primary items (max 4 + more)
  const mobilePrimary = [
    { page: 'dashboard', label: 'แดชบอร์ด', icon: 'fa-tachometer-alt', href: '/admin/dashboard', permission: 'menu:dashboard' },
    { page: 'chat', label: 'แชท', icon: 'fa-comments', href: '/admin/chat', badgeId: 'chatNotificationBadge', permission: 'menu:chat' },
    { page: 'orders', label: 'ออเดอร์', icon: 'fa-shopping-basket', href: '/admin/orders', permission: 'menu:orders' },
    { page: 'settings2', label: 'ตั้งค่า', icon: 'fa-cogs', href: '/admin/settings2', permission: 'menu:settings' },
  ].filter((item) => canAccess(item.permission));
  const visibleNavGroups = navGroups
    .map((group) => ({ ...group, items: group.items.filter((item) => canAccess(item.permission)) }))
    .filter((group) => group.items.length > 0);
  const homeHref = visibleNavGroups[0]?.items?.[0]?.href || '/admin/chat';
%>

<!-- Layout CSS/JS -->
<link href="/css/admin-layout.css" rel="stylesheet">
<script src="/js/admin-font.js"></script>
<script>
  /* FOUC prevention: add layout class immediately before first paint */
  (function () {
    var b = document.body;
    var currentPage = '<%= current %>';
    var storedSidebarState = null;
    var isSidebarCollapsed = true;
    try {
      storedSidebarState = window.localStorage.getItem('adminSidebarCollapsedV2');
      isSidebarCollapsed = storedSidebarState === null ? true : storedSidebarState !== 'false';
    } catch (_) {
      isSidebarCollapsed = true;
    }
    if (b) {
      b.classList.add('has-admin-layout');
      b.classList.toggle('sidebar-collapsed', isSidebarCollapsed);
      if (currentPage) {
        b.classList.add('page-' + currentPage.toLowerCase().replace(/[^a-z0-9-]/g, '-'));
      }
    }
  })();
  window.__adminActivePage = '<%= current %>';
  window.adminAuth = window.adminAuth || {};
  window.adminAuth.requirePasscode = <%= typeof isPasscodeRequired !== 'undefined' ? (isPasscodeRequired ? 'true' : 'false') : 'false' %>;
  window.adminAuth.user = <%- JSON.stringify(signedInUser || null).replace(/</g, '\\u003c') %>;
</script>
<script src="/js/admin-layout.js" defer></script>

<!-- ======================== Top Bar ======================== -->
<header class="app-topbar" id="appTopbar">
  <div class="app-topbar__left">
    <!-- Desktop sidebar toggle -->
    <button class="app-topbar__toggle d-none d-lg-inline-flex" data-layout-toggle="desktop" data-desktop-toggle aria-label="พับ/ขยายเมนูด้านข้าง">
      <i class="fas fa-bars"></i>
    </button>
    <!-- Mobile sidebar toggle -->
    <button class="app-topbar__toggle d-lg-none d-inline-flex" data-layout-toggle="mobile" data-mobile-toggle aria-label="เปิดเมนูด้านข้าง">
      <i class="fas fa-bars"></i>
    </button>
    <a class="app-topbar__brand d-none d-sm-flex" href="/admin/dashboard" aria-label="Voxtron">
      <img
        class="app-topbar__brand-logo"
        src="/assets/brand/voxtron-logo.png"
        alt="Voxtron"
      >
    </a>
    <span class="app-topbar__title"><%= pageTitle %></span>
  </div>

  <div class="app-topbar__spacer"></div>

  <div class="app-topbar__right">
    <select id="globalFontSelector" class="app-topbar__font d-none d-lg-block">
      <option value="'Mali', 'Inter', sans-serif">Mali</option>
      <option value="'Sarabun', sans-serif">Sarabun</option>
      <option value="'Krub', sans-serif">Krub</option>
      <option value="'Mitr', sans-serif">Mitr</option>
      <option value="'Pridi', serif">Pridi</option>
      <option value="'Niramit', sans-serif">Niramit</option>
    </select>

    <% if (signedInUser) { %>
      <span class="app-topbar__role">
        <i class="fas fa-user-shield"></i>
        <%= roleBadgeLabel %>
        <% if (signedInUser.label) { %>
          <span class="text-muted">&middot; <%= signedInUser.label %></span>
        <% } %>
      </span>
      <button class="app-topbar__logout" id="adminLogoutBtn" title="ออกจากระบบ" aria-label="ออกจากระบบ">
        <i class="fas fa-sign-out-alt"></i>
      </button>
    <% } %>
  </div>
</header>

<!-- ======================== Sidebar (Desktop) ======================== -->
<aside class="app-sidebar" id="appSidebar">
  <div class="app-sidebar__scroll">
    <% visibleNavGroups.forEach((group, gIdx) => { %>
      <div class="app-sidebar__section-title"><%= group.title %></div>
      <nav class="app-sidebar__nav" aria-label="<%= group.title %>">
        <% group.items.forEach((item) => { %>
          <a
            href="<%= item.href %>"
            class="app-sidebar__item <%= isActive(item.page) ? 'is-active' : '' %>"
            data-page="<%= item.page %>"
            data-label="<%= item.label %>"
            aria-label="<%= item.label %>"
          >
            <i class="fas <%= item.icon %>"></i>
            <span class="app-sidebar__text"><%= item.label %></span>
            <% if (item.badgeId) { %>
              <span class="app-sidebar__badge" id="<%= item.badgeId %>" data-chat-badge style="display:none">0</span>
            <% } %>
          </a>
        <% }) %>
      </nav>
    <% }) %>
  </div>

  <div class="app-sidebar__footer">
    <a class="app-sidebar__back" href="<%= homeHref %>">
      <i class="fas fa-arrow-left"></i>
      <span>กลับหน้าแดชบอร์ด</span>
    </a>
  </div>
</aside>

<!-- ======================== Mobile Bottom Nav ======================== -->
<nav class="app-mobile-nav" id="appMobileNav" aria-label="เมนูหลัก">
  <div class="app-mobile-nav__inner">
    <% mobilePrimary.forEach((item, idx) => { %>
      <% if (idx > 0) { %>
        <div class="app-mobile-nav__divider" aria-hidden="true"></div>
      <% } %>
      <a
        href="<%= item.href %>"
        class="app-mobile-nav__item <%= isActive(item.page) ? 'is-active' : '' %>"
        data-page="<%= item.page %>"
      >
        <i class="fas <%= item.icon %>"></i>
        <span class="app-mobile-nav__label"><%= item.label %></span>
        <% if (item.badgeId) { %>
          <span class="app-mobile-nav__badge" data-chat-badge style="display:none">0</span>
        <% } %>
      </a>
    <% }) %>

    <div class="app-mobile-nav__divider" aria-hidden="true"></div>

    <button class="app-mobile-nav__item" id="appMobileNavMore" type="button" aria-label="เมนูอื่น ๆ">
      <i class="fas fa-ellipsis-h"></i>
      <span class="app-mobile-nav__label">อื่น ๆ</span>
    </button>
  </div>
</nav>

<!-- Mobile More Sheet -->
<div class="app-mobile-nav__more-sheet" id="appMobileNavMoreSheet" aria-hidden="true">
  <div class="app-mobile-nav__more-backdrop"></div>
  <div class="app-mobile-nav__more-panel">
    <div class="app-mobile-nav__more-handle" aria-hidden="true"></div>
    <% visibleNavGroups.forEach((group) => { %>
      <div class="app-mobile-nav__more-title"><%= group.title %></div>
      <div class="app-mobile-nav__more-grid">
        <% group.items.forEach((item) => { %>
          <a
            href="<%= item.href %>"
            class="app-mobile-nav__more-item <%= isActive(item.page) ? 'is-active' : '' %>"
            data-page="<%= item.page %>"
          >
            <i class="fas <%= item.icon %>"></i>
            <span><%= item.label %></span>
          </a>
        <% }) %>
      </div>
    <% }) %>
  </div>
</div>

<!-- ======================== Overlay ======================== -->
<div class="app-sidebar-overlay" id="appSidebarOverlay"></div>
